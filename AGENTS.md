# AGENTS.md

本项目共享约定与规格文档，供 Agent（及子代理）在本仓库工作时遵守。

## 项目目标
- 需求方想通过"抽奖"的方式给老爸送一台 vivo X200s 手机（直接买不愿接受）。
- 做一个**手机端抽奖网页**（PC 测试、最终微信内 Android 打开使用），简约 → 已升级为多彩炫酷风。

## 核心规格（已与用户确认，勿改）
- **登录**：首页登记姓名 + 手机号；手机号仅校验 11 位长度。
- **抽奖次数**：初始 **3 次**；**转发无限加次**（无每日上限），可无限次数抽。
- **转发**：点按钮 → 提示"已复制到剪贴板"（不真正复制）→ **10 秒倒计时**后次数 +1；用户中途退出网页也不丢次数（pending 任务持久化，回来后补发）。
- **奖品与概率**：8 槽位轮盘，各扇区概率均等。手机×1（vivo X200s 黑 12G+256G）、5元话费券×1、10元话费券×1、感谢参与×5。
  - 手机：概率 **1/8**，**仅两个指定号**可中，且须**第 5 次起**才进抽奖池。
  - 指定号：`13925599313`（老爸）、`19868735951`（用户测试号）。
  - **老爸号**中手机后**封抽不能再抽**；**测试号不封**可继续抽。
  - 话费券/感谢参与可中无数次，不影响；其他所有号码永远中不了手机。
- **奖品发放**：中话费券直接弹窗"恭喜中奖"；**仅中手机**弹收货地址表单（自由填写，无省市区）。
- **个人中心**：未中手机显示无奖品；中手机则显示奖品 + "待收取"。
- **界面**：多彩/丰富（已弃极简），轮盘式，抽奖按钮有按下动效，中/未中都弹窗，中手机动画绚烂华丽（金色粒子+彩带）。

## 数据与存储
- 纯前端 **localStorage**，**按手机号**存储；退出网页再进自动记忆当前账号。
- dev 阶段有退出登录按钮；**正式版需把 `js/app.js` 顶部 `CONFIG.IS_DEV` 改为 `false`**（隐藏退出按钮）。
- 不做重后端、不防作弊，轻量即可。
- 可调配置均在 `js/app.js` 顶部 `CONFIG`：`PHONE_DRAW_START`（手机进池次数）、`INIT_TIMES`（初始次数）、`FORWARD_WAIT`（转发等待秒数）、`SPECIAL`/`TEST`（指定号）。

## 架构与文件结构
```
抽奖系统/
├── AGENTS.md
├── index.html            正式版入口（登录 / 抽奖 / 个人中心 / 各弹窗）
├── css/style.css         主题 + 动效（CSS 变量，金色奢华底 + 多彩光晕）
├── js/app.js             全部业务逻辑（Canvas 轮盘引擎 + 抽奖/转发/存储）
├── README.md             项目背景与需求说明
├── DEPLOY.md             部署说明（GitHub Pages / 对象存储 / Nginx，微信要求 HTTPS）
└── previews/             界面风格 demo（供用户三选一）
    ├── demo1.html 经典豪华 demo2.html 多彩明快 demo3.html 霓虹炫彩
    └── demo-core.js      共享 Canvas 轮盘引擎（演示用）
```

## 关键实现要点
- **轮盘 = Canvas 一体绘制**：扇区 + 真实奖品图片（SVG data-URI，离线可用）+ 径向文字在同一 Canvas，仅用 CSS `rotate` 旋转，保证"指针所指 = 中奖结果"严格一致。**不要**再用 SVG 轮盘 + 独立 HTML 图标覆盖层（会导致错位）。
- 奖品图片为内联 SVG：`svgPhone` / `svgCoupon(金额, 色1, 色2)` / `svgThanks`，转 data-URI。
- 奖品槽位下标固定：`0`=手机、`3`=5元券、`6`=10元券，其余为感谢参与。
- `rollIndex(user, drawNo)` 负责中奖加权判定（含"第5次起+特殊号+未中手机"才加入手机权重）。
- 转发倒计时用 `ll_pending_fwd` 持久化，登录/启动时 `resumeForward()` 续接补发。

## 验证方法
- 语法检查：`node --check js/app.js`
- 打开 `index.html` 手动测试：
  - 任意号码：只能中话费券 / 感谢参与。
  - `13925599313`：第 5 次起有 1/8 概率中手机，中后封抽。
  - `19868735951`：可中手机且不封抽（测试用）。
- 检查轮盘指针所指扇区与弹窗结果是否一致。

## 注意
- 回复用**中文**，保持简洁。
- 改文件前先说明"改什么、为什么"（符合反射门要求）。
- 涉及新增改动时，确保 `index.html` 中所有被 `js/app.js` `$()` 引用的 id 均存在。
